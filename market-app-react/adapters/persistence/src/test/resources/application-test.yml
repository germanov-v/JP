spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/market_reactive_test
    username: postgres
    password: postgres
    pool:
      enabled: true
      initial-size: 1
      max-size: 5

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
    url: jdbc:postgresql://localhost:5432/market_reactive_test
    user: postgres
    password: postgres

logging:
  level:
    org.springframework.r2dbc: debug